services:
  - type: web
    name: assistente-cpz
    runtime: node
    rootDir: assistente-cpz/backend
    buildCommand: npm ci
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
