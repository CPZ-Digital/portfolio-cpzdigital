services:
  - type: web
    name: assistente-cpz
    runtime: node
    rootDir: assistente-cpz/backend
    buildCommand: npm install
    startCommand: bash start.sh
    envVars:
      - key: NODE_ENV
        value: production
